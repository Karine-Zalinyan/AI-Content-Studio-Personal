# Stock + Avatar UI integration

The next browser slice should expose clip selection and optional Avatar reference in the existing Social Content Studio page, then call `StockAvatarWebService` to assemble a 1080x1920 MP4 and persist it in History. The paid generation path remains unchanged.
